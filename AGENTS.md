# Repository Agent Guidelines

## Git & Commit Policy
- **NEVER** commit or push changes automatically.
- Only run `git commit` or `git push` when the user explicitly instructs you to commit or push in chat.
- Rebuilding, packaging (`npm run package`), and installing the local `.vsix` for testing is fine, but keep git changes unstaged/uncommitted until instructed.
